apiVersion: v1
kind: Service
metadata:
  name: backend-api
  namespace: opswatch
  labels:
    app: backend-api
    app.kubernetes.io/name: opswatch
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: backend-api
  ports:
    - name: http
      port: 3001
      targetPort: http
