apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: opswatch
  labels:
    app.kubernetes.io/name: opswatch
    app.kubernetes.io/component: backend
data:
  PORT: "3001"
  NODE_ENV: production
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
